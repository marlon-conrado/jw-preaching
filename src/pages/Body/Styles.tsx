import styled from "styled-components";

const BodyContainer = styled.div`
  /* altura de la pantalla menos altura de la barra de navegación */
  height: calc(100vh);
  overflow-y: auto;
`;

export default BodyContainer;
